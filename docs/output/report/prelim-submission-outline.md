# 初赛提交大纲

截止：2026-07-26 24:00  
用途：填茶思屋字段前的写作骨架（≤800 字介绍 + 测试报告雏形）。

## 1. 一句话创意

基于比特级查找表的系统级低比特 LLM 加速服务，结合用户行为感知实现动态资源调度。

## 2. 设计稿应包含

| 图 | 内容 |
|----|------|
| 系统架构 | LUT kernel → Native 库 → SystemAbility → 调度策略 → 上层 AI 任务 |
| 数据/交互流 | 感知触发 → 优先级/预取 → 加速推理 → 指标回传 |

落盘建议：定稿图放 `assets/images/readme/` 或 `assets/theme/ppt/`；源文件可放 `assets/backup/`。

## 3. 作品介绍（≤800 字）建议结构

1. 行业痛点：边缘 LLM 反量化开销、能耗与 TTFT
2. 核心设计：T-MAC LUT 范式 + 鸿蒙/OH 系统服务封装
3. 技术路径：NDK 库 → SA 接口 → 轻量调度
4. 预期指标：引用公开评测并说明可移植性（标明来源）
5. 落地价值：端侧部署、低功耗、可演示对比

## 4. 测试报告雏形（目录）

1. 测试环境（公开 CPU / 模拟器计划）
2. 基线：llama.cpp
3. 指标：tokens/s、TTFT、线程数、功耗/能耗（能测则测）
4. 结果表（可先填论文/本仓 `docs/profiling_data.md` 公开数据）
5. 移植到 OpenHarmony 的计划与风险
6. 复现步骤（命令级，能写多少写多少）

正文草稿可开 GitHub Issue 跟踪，定稿落 `docs/output/report/`。
